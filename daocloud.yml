image: centos:centos7

services:

env:

install:
    - pwd
    - ls -la .
    - sudo /bin/bash ./ci/install.sh

before_script:	
    - echo "Setup"

script:
    - cd webapp
    - /usr/local/maven/bin/mvn test
